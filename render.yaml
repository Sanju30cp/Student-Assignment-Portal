services:
  - type: web
    name: student-assignment-portal
    runtime: python
    buildCommand: chmod +x build.sh && ./build.sh
    startCommand: gunicorn assignment_portal.wsgi:application
    envVars:
      - key: PYTHON_VERSION
        value: 3.11.1
